<Window x:Class="WPFMusicPlayer.View.PlaylistNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="PlaylistNameDialog" Height="200" Width="200">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="0, 20, 0 ,0">
            <TextBlock HorizontalAlignment="Center">Enter a name for your new playlist</TextBlock>
            <TextBox Text="{Binding TextBoxText}" Margin="0, 10, 0 ,0"/>
            <Button Width="30" Height="20" Margin="0, 10, 0 ,0" Command="{Binding OkCommand}">Ok</Button>
        </StackPanel>
    </Grid>
</Window>
